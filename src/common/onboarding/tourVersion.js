// Bump this whenever tour content changes enough that returning users should
// see it again. Intentionally independent of the app's package.json version —
// not every release ships a new tour.
export const CURRENT_TOUR_VERSION = "1";
