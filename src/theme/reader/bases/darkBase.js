import { dark as darkColors } from "@theme/semanticColors";
import createAppBase from "./appBase";

// Today's dark Reader. Mirrors lightBase — see appBase.js.
export default createAppBase(darkColors, "dark");
